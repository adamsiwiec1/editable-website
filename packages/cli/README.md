# editable-website CLI

Interactive bootstrapper. Detects the host framework, finds static copy, highlights what cannot be auto-marked, and writes `data-copy` plus a `mount()` snippet.

```bash
npx editable-website
npx editable-website ./my-site --yes --dry-run
```

Zero npm dependencies. Published as the `editable-website` bin on the core package.
