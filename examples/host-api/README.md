# Host API

The package does not ship login or storage. Your app exposes `GET` / `PUT /api/copy`.

The runnable example is **[examples/express](../express)** — Express, JSON file, no database.

This folder is only a tiny handler sketch if you are not using Express: [copy-handler.mjs](copy-handler.mjs).
