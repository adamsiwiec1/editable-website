import type { ReactNode } from 'react';

export const metadata = {
  title: 'editable-website · Next.js demo · hidden login',
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
